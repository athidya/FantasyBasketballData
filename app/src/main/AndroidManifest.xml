<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.athidya.mydataapp">

    <uses-permission android:name="android.permission.INTERNET" />
    <!-- To auto-complete the email text field in the login form with the user's emails -->
    <uses-permission android:name="android.permission.GET_ACCOUNTS" />
    <uses-permission android:name="android.permission.READ_PROFILE" />
    <uses-permission android:name="android.permission.READ_CONTACTS" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".MainActivityStart"
            android:label="@string/app_name"
            android:theme="@style/AppTheme.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />

                <data android:host="oob" />
                <data android:scheme="oob" />
            </intent-filter>
        </activity>
        <!--
 ATTENTION: This was auto-generated to add Google Play services to your project for
     App Indexing.  See https://g.co/AppIndexing/AndroidStudio for more information.
        -->
        <meta-data
            android:name="com.google.android.gms.version"
            android:value="@integer/google_play_services_version" />

        <activity
            android:name=".loginyahoo"
            android:label="@string/title_activity_loginyahoo"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".code"
            android:label="@string/title_activity_code"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity android:name=".SathActivity" />
        <activity android:name=".SelectionActivity" />
        <activity
            android:name=".StatViewerActivity"
            android:label="@string/title_activity_stat_viewer"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".PredictionsActivity"
            android:label="@string/title_activity_predictions"
            android:theme="@style/AppTheme.NoActionBar"></activity>
    </application>

</manifest>